<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AdventureList</title>
</head>
<body>
<div class="container">
    <h1>AdventureList</h1>
    <a href="/bucketlist/new">Create a new Item</a>
    <form action="/user/logout?_method=DELETE" method="POST">
        <input type="submit" value="Logout" />
    </form>
    <table>
        <% for(let i = 0; i < Bucket.length; i++) { %>
        <tr>
            <td> 
                <input type="checkbox" name="completed"
                <%= Bucket[i].completed ? 'checked' : 'unchecked' %>>
            </td>
            <td> <a href="/bucketlist/list/<%=Bucket[i].id%>"> <%= Bucket[i].title %> </a></td>
            <td><form action="/bucketlist/list/<%=Bucket[i].id%>?_method=DELETE" method="POST">
                <input type="submit" value="DELETE">
            </form></td>
        </tr>
        <% } %>
    </table>
 </div>
</body>
</html>